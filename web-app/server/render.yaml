services:
  - type: web
    name: tg-downloader-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn py_backend:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PORT
        value: 5000
